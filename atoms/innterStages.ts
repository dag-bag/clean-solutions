/** @format */

import { atom } from "recoil";
export const innnerStagesAtom = atom({
  key: "innnerStagesAtom",
  default: 0,
});

export const deepStateAtom = atom({
  key: "deepStateAtom",
  default: 0,
});
